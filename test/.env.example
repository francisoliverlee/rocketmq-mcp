# RocketMQ MCP 单元测试环境变量配置示例
# 复制此文件为 .env 并修改相应的值

# MCP服务器配置
MCP_SERVER_URL=http://localhost:6868

# NameServer配置
NAMESERVER_ADDR=127.0.0.1:9876

# 认证信息
MCP_AK=your_access_key
MCP_SK=your_secret_key

# Broker配置
BROKER_ADDR=127.0.0.1:10911

# 测试数据配置
TEST_TOPIC=test_topic
TEST_CONSUMER_GROUP=test_consumer_group
TEST_PRODUCER_GROUP=test_producer_group
TEST_CLIENT_ID=test_client_id
TEST_BROKER=test_broker
TEST_CLUSTER=test_cluster

# 其他测试配置
TEST_MESSAGE_ID=test_message_id
TEST_MESSAGE_KEY=test_message_key
TEST_UNIQUE_KEY=test_unique_key

# 使用说明：
# 1. 将此文件复制为 .env
# 2. 修改相应的配置值为您的实际环境值
# 3. 运行测试时，系统会自动读取这些环境变量
# 4. 如果没有设置环境变量，将使用默认的测试值